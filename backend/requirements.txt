fastapi
uvicorn[standard]
pydantic-settings
python-multipart
python-dotenv
firebase-admin
google-cloud-storage
google-cloud-run
redis
rq
opencv-python-headless
Pillow>=10.0,<13
numpy
tensorflow
tensorflow-metal; sys_platform == "darwin" and platform_machine == "arm64"
tf-keras
pandas
scikit-learn
joblib
scipy
rapidfuzz
requests
# Python wrapper around the system `tesseract` OCR binary (installed via
# apt in Dockerfile / `brew install tesseract` locally) — app/ml/ocr.py
# imports this unconditionally, so it's not optional.
pytesseract
torch
torchvision
transformers
hdbscan
